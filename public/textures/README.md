# Texture library

Commercial packs stay local. Regenerated by:

```bash
npm run import-textures
```

- `thumbs/` is committed so the gallery has previews
- `full/` is gitignored (large, licensed rasters)
- Catalog lives in `src/lib/textureCatalog.generated.ts`
